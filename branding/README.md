# Kakomi branding

## Source of truth

The logo shown in `kakomi-brand-video.mp4` is sourced directly from `kakomi.svg`. Treat `kakomi.svg` as the canonical Kakomi logo: preserve its geometry and colors, and regenerate raster or promotional variants from it rather than redrawing the mark.

- `kakomi.svg` — canonical vector logo
- `kakomi-icon.png` — raster icon derived from the canonical vector
- `kakomi-brand-video.mp4` — official branding video using the canonical logo
